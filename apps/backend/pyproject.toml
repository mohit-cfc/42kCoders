[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "contracts",
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "mcp>=0.1.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.1"
]

[tool.uv.sources]
contracts = { workspace = true }

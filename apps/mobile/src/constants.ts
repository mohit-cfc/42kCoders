// Seed center matches apps/backend/scripts/seed.py defaults (Marol, Mumbai —
// Waterstones Hotel). useLocation falls back to this until real GPS is wired.
export const DEMO_CENTER = { lat: 19.10598, lng: 72.86016 };
